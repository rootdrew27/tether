kind = "agent_config"
name = "tether-sonnet"
description = "Claude Code (sonnet) with tether installed, initialized, and Claude Code hooks active in the case workspace"
definition = "../../agent-definitions/claude-code-tether"
mode = "direct"

[input]
claude_version = "2.1.167"
startup_args = ["--model", "claude-sonnet-4-6", "--effort", "medium"]
run_args = []
settings_json = """
{
  "model": "claude-sonnet-4-6",
  "permissionMode": "acceptEdits"
}
"""
